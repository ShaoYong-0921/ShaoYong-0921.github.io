# 開發日誌 2026-07-11

M3 收官日（剩派加固一張卡）。**最初的三大願望全數達成**：網站直連 code-server（/lab）、同步本地 Obsidian（LiveSync）、筆記渲染上 blog（自動管線）。

## 今日完成

| 項目 | 產出 |
|---|---|
| F16 備份 + 還原演練 | `scripts/backup/backup.sh` 每日 03:30（保留 7 份）；**演練實測通過**（還原庫 doc_count 177=177）；災難還原 SOP 入庫 |
| CouchDB 密碼輪換 | 舊密碼（曾入截圖/對話）作廢；驗證新通舊擋 |
| code-server 容器化 | 進 infra compose（自建映像含 Node22/pnpm/Python），HTTPS + 只綁 Tailscale；原生 service 停用；設定/擴充無痛沿用 |
| 憑證自動續期 | 過期 3 天的 Tailscale 憑證換新；`cert-renew.timer` 每月自動續 + 重載；`tailscale set --operator` 免 sudo |
| /lab 頁（F8） | https://shaoyong.dev/lab/ ：code-server 直開連結（主機→容器路徑映射）+ 內部工具；不入導覽列與 sitemap |
| CouchDB HTTPS 門面 | `tailscale serve` → https://homepi.tail890983.ts.net:8443（憑證全自動）；Windows Obsidian 已切換；iPad 入場券（Setup URI）已備 |
| remark-breaks | 單一換行=真換行，網站渲染貼齊 Obsidian 所見即所得 |

## 事件與教訓

1. **密碼輪換三層迷宮**：舊密碼怎麼都殺不死——(a) CouchDB 把管理員雜湊持久化到「設定鏈最後一個檔案」＝我們掛載的 livesync.ini（改 `00-` 前綴讓 docker.ini 墊後）；(b) 該雜湊還被 commit 進了公開 repo（掛載檔要在**首次啟動前**就 commit 乾淨版）；(c) 官方映像的 `local.d` 是匿名 volume，重建要 `--renew-anon-volumes`。附帶：entrypoint 會 chown 設定目錄，掛載不能 `:ro`（容器會無日誌崩潰）。
2. **容器終端提示字元變成 `coder@容器ID`**，使用者不滿。修復：compose `hostname:` + 自訂 .bashrc。教訓：**遷移前先列出使用者看得到的變化**，能保留就保留。
3. **兩地副本再度咬人**：在派上 biome --write 修了 lab.astro，scratchpad 舊副本後續上傳蓋掉修復 → CI 紅燈（門檻正確擋下部署）。規則升級：派上就地改過的檔案，動 scratchpad 前必先拉回。
4. **Markdown 單換行不換行**：Obsidian 顯示與標準 Markdown 語意不同。決策：加 remark-breaks 讓網站遷就寫作工具（寫作主場是 Obsidian，所見即所得優先）。
5. tailscale serve 依網域名服務，用 IP 直連 8443 會失敗——正常行為，不是故障。

## 下次的卡

1. **派加固**（M3 最後一張）：關 lightdm、ufw、fail2ban、實體散熱（待機 57°C 偏高）——需 sudo 陪同
2. iPad 實機接入（兩個 App + Setup URI，隨時可做）
3. 異地加密備份（Issue）；M4 FastAPI 統計 API（主線下一站）
4. 技術文素材已滿：Astro 7 移植記、自架 Obsidian 同步發佈管線
