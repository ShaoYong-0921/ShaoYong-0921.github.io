# 開發日誌 2026-07-12 —— M4、M5 收官，v1 完成

## M4：第一個公開 API（三階段）

**① 設計＋實作**
- `docs/design/views-api.md` 定案（spec §9 最後缺口關閉）：三 endpoint、slug 驗證沿用發佈規則、**零個資**（只存 slug+次數）、前端漸進增強
- `api/`：FastAPI + SQLite（WAL），6 個 unittest；派上容器實測全過（計數、只讀、擋非法輸入）

**② CI/CD 全自動**
- `api-image.yml`：測試綠 → QEMU 建 arm64 → 推 ghcr.io（latest + SHA 雙標籤，可回滾）
- Watchtower 依 label 每 5 分鐘巡邏
- **實彈演習**：push 1.0.1 後全程不碰派——`Found new image → SIGTERM → Creating → Session done Failed=0 Updated=1`，約 10 分鐘自動上線 ✅

**③ 公開與顯示**
- Cloudflare Tunnel `homepi`（cloudflared 容器，4 條 QUIC 連線：tpe01×2、khh01×2）→ `https://api.shaoyong.dev`；**派零公網 port**（spec §3 原則完好）
- 文章頁 meta 列新增「👁 N 次瀏覽」：sessionStorage 同工作階段去重、API 失敗即靜默隱藏；已知限制——swup 站內導航不重新計數

**M4 DoD**：每頁顯示瀏覽次數 ✅、公網可呼叫 ✅、CI/CD 全自動 ✅（實證）

## M5：即時筆記牆（選做，也做完了）

- Perlite（nginx → php-fpm 雙容器）；官方 compose 用已淘汰的 `volumes_from`，改寫為共享 named volume `perlite-app`（**注意：更新 Perlite 版本需 `docker volume rm homepi_perlite-app` 重建**）
- 只掛 `vault/garden:ro`——私密筆記**物理不可達**，不是設定攔截
- **DoD 驗收**：牆可達（僅 Tailscale）✅、筆記存檔即時可見（零建置）✅、`notes/` 直接存取與路徑穿越均 403 ✅
- /lab 新增入口（🌱）

## 今日事件與教訓

1. **Watchtower 化石映像**：`containrrr/watchtower:latest` 停止維護，內建 Docker client API 1.25 被新 daemon 拒絕（min 1.40）→ 設 `DOCKER_API_VERSION=1.44` 復活。服務本體全程不受影響——自動更新器壞掉只是「暫停更新」，解耦的價值
2. **root 的 `__pycache__` 毀了一次 rebase**：測試容器以 root 跑、往掛載目錄寫入 root 檔案 → git 連環卡死。復原：強拆 rebase、reset 到 origin、從 scratchpad 重建。預防：`api/.gitignore` + 測試容器改用非 root
3. **compose 頂層 `volumes:` 插錯位置**（服務中間）→ 之後任何 compose 修改先 `docker compose config -q` 驗證再 up
4. 驗證要等 CDN：部署成功 ≠ 立即可見，快取有一兩分鐘尾巴

## v1 完成總結（重構前基準線）

| 里程碑 | 原時程 | 實際 | DoD |
|---|---|---|---|
| M1 網站骨架 | 週 1–2 | 第 2 天 | 全達成 |
| M2 筆記 pipeline | 週 3–4 | 第 3 天 | 全達成（F13 多次實戰） |
| M3 派同步中樞 | 週 5–7 | 第 4 天 | 達成（加固一項使用者取消） |
| M4 公開 API | 週 8+ | 第 5–6 天 | 全達成（實證） |
| M5 筆記牆 | 選做 | 第 6 天 | 全達成 |

**運行中的服務**（compose，8 容器）：couchdb、livesync-bridge、code-server、views-api、cloudflared、watchtower、perlite、perlite-web
**排程**（systemd user timers）：vault-publish（5 分）、vault-backup（每日）、cert-renew（每月）
**文件資產**：規格書 v2、5 份設計文件、6 篇日誌、README——全程可追溯

**此日誌系列即 v1 的完整記錄。下一步：重構（範圍與目標另行規劃）。**
